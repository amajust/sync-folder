name: Build Sync-App Package

on:
  push:
    paths:
      - 'sync-app/**'
  pull_request:
    paths:
      - 'sync-app/**'

jobs:
  build-sync-app:
    runs-on: ubuntu-latest

    container:
      image: archlinux:latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v4

      # Step 2: Set up a non-root user for makepkg
      - name: Set Up Non-Root User
        run: |
          useradd -m builder
          passwd -d builder
          echo 'builder ALL=(ALL) NOPASSWD: ALL' >> /etc/sudoers

      # Step 3: Install dependencies
      - name: Install Dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm base-devel rsync inotify-tools

      # Step 4: Switch to non-root user and build the package
      - name: Build Sync-App Package
        run: |
          cd sync-app
          su builder -c "makepkg -sf --noconfirm"

      # Step 5: Upload the built package as an artifact
      - name: Upload Package Artifact
        uses: actions/upload-artifact@v4
        with:
          name: sync-app-package
          path: sync-app-*.pkg.tar.zst

